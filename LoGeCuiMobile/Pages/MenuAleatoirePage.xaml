<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LoGeCuiMobile.Pages.MenuAleatoirePage"
             Title="Menu aléatoire">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <Button
                Text="🎲 Menu aléatoire"
                Clicked="OnMenuAleatoireClicked" />

            <Label
                x:Name="ResultLabel"
                Text="Appuyez sur le bouton pour générer un menu"
                FontSize="18"
                HorizontalTextAlignment="Center" />

            <!-- Bouton visible uniquement après génération si ingrédients manquants -->
            <Button x:Name="SendMissingButton"
                    Text="🛒 Envoyer les ingrédients manquants dans la liste de courses"
                    Clicked="OnSendMissingClicked"
                    HorizontalOptions="FillAndExpand"
                    IsVisible="False"
                    IsEnabled="False" />

            <Button
                Text="🛒 Ouvrir la liste de courses"
                Clicked="OnOpenListeCoursesClicked"
                HorizontalOptions="FillAndExpand" />

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>



<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:tr="clr-namespace:LoGeCuiMobile.Resources.Lang"
             x:Class="LoGeCuiMobile.Pages.SettingsPage"
             Title="{tr:Translate Settings_Title}">

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="16">

            <!-- ========================= -->
            <!-- PARTAGE LISTE DE COURSES  -->
            <!-- ========================= -->
            <Label Text="{tr:Translate Auto_Liste_De_Courses_Partagee}"
                   FontSize="18"
                   FontAttributes="Bold" />

            <Frame Padding="12"
                   CornerRadius="12"
                   BorderColor="#DDDDDD"
                   HasShadow="True">
                <VerticalStackLayout Spacing="10">

                    <Label Text="{tr:Translate Auto_Liste_Active}"
                           FontAttributes="Bold" />

                    <Label x:Name="LblActiveList"
                           Text="(aucune)"
                           TextColor="Gray" />

                    <Label Text="{tr:Translate Auto_Code_De_Partage}"
                           FontAttributes="Bold"
                           Margin="0,6,0,0"/>

                    <Label x:Name="LblShareCode"
                           Text="(aucun)"
                           TextColor="Gray" />

                    <HorizontalStackLayout Spacing="10">
                        <Button Text="{tr:Translate Auto_Creer_Voir_Mon_Code}"
                                Clicked="OnCreateOrShowMyCodeClicked"
                                HorizontalOptions="FillAndExpand"/>

                        <Button Text="{tr:Translate Auto_Copier}"
                                Clicked="OnCopyCodeClicked"
                                HorizontalOptions="End"/>
                    </HorizontalStackLayout>

                    <Button Text="{tr:Translate Auto_Rejoindre_Avec_Un_Code}"
                            Clicked="OnJoinWithCodeClicked" />

                    <Button Text="{tr:Translate Auto_Choisir_Une_Liste_Active}"
                            Clicked="OnPickActiveListClicked" />

                    <Button Text="{tr:Translate Auto_Revenir_A_Ma_Liste_Perso}"
                            BackgroundColor="#E67E22"
                            TextColor="White"
                            Clicked="OnDisableSharedListClicked" />

                </VerticalStackLayout>
            </Frame>

            <!-- ========================= -->
            <!-- COMPTE                    -->
            <!-- ========================= -->
            <Label Text="{tr:Translate Settings_Account}"
                   FontSize="18"
                   FontAttributes="Bold"
                   Margin="0,10,0,0" />

            <Frame Padding="12"
                   CornerRadius="12"
                   BorderColor="#DDDDDD"
                   HasShadow="True">
                <VerticalStackLayout Spacing="12">

                    <Button Text="{tr:Translate Settings_Logout}"
                            BackgroundColor="#34495E"
                            TextColor="White"
                            FontAttributes="Bold"
                            Clicked="OnLogoutClicked" />

                    <Label Text="{tr:Translate Settings_DangerZone}"
                           FontSize="14"
                           TextColor="Gray"
                           Margin="0,6,0,0" />

                    <Button Text="{tr:Translate Settings_Unsubscribe}"
                            BackgroundColor="IndianRed"
                            TextColor="White"
                            FontAttributes="Bold"
                            Clicked="OnDesinscriptionClicked" />

                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>

<UserControl x:Class="LoGeCui.Views.MenuAleatoireView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- TITRE -->
            <TextBlock Grid.Row="0"
                       Text="🎲 Menu Aléatoire"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="#2C3E50"
                       Margin="0,0,0,20"/>

            <!-- BOUTON GENERER -->
            <Button Grid.Row="1"
                    Content="🎲 Générer un nouveau menu"
                    Width="250"
                    Height="50"
                    Background="#9B59B6"
                    Foreground="White"
                    FontSize="16"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="BtnGenererMenu_Click"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,30">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#9B59B6"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#8E44AD"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <!-- CONTENU DU MENU -->
            <StackPanel Grid.Row="2" x:Name="MenuPanel" Visibility="Collapsed">

                <!-- Date -->
                <TextBlock x:Name="TxtDate"
                           FontSize="16"
                           Foreground="#7F8C8D"
                           Margin="0,0,0,20"
                           HorizontalAlignment="Center"/>

                <!-- Entrée -->
                <Border Background="#E8F5E9"
                        CornerRadius="10"
                        Padding="20"
                        Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="🥗 ENTRÉE"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="#27AE60"
                                   Margin="0,0,0,10"/>

                        <TextBlock x:Name="TxtEntree"
                                   Text="Aucune entrée"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#2C3E50"
                                   Margin="0,0,0,5"/>

                        <TextBlock x:Name="TxtEntreeDetails"
                                   FontSize="14"
                                   Foreground="#7F8C8D"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Plat -->
                <Border Background="#E3F2FD"
                        CornerRadius="10"
                        Padding="20"
                        Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="🍽️ PLAT PRINCIPAL"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="#2196F3"
                                   Margin="0,0,0,10"/>

                        <TextBlock x:Name="TxtPlat"
                                   Text="Aucun plat"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#2C3E50"
                                   Margin="0,0,0,5"/>

                        <TextBlock x:Name="TxtPlatDetails"
                                   FontSize="14"
                                   Foreground="#7F8C8D"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Dessert -->
                <Border Background="#FFF3E0"
                        CornerRadius="10"
                        Padding="20"
                        Margin="0,0,0,20">
                    <StackPanel>
                        <TextBlock Text="🍰 DESSERT"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="#FF9800"
                                   Margin="0,0,0,10"/>

                        <TextBlock x:Name="TxtDessert"
                                   Text="Aucun dessert"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Foreground="#2C3E50"
                                   Margin="0,0,0,5"/>

                        <TextBlock x:Name="TxtDessertDetails"
                                   FontSize="14"
                                   Foreground="#7F8C8D"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Ingrédients manquants -->
                <Border x:Name="IngredientsManquantsPanel"
                        Background="#FFEBEE"
                        CornerRadius="10"
                        Padding="20"
                        Margin="0,0,0,20"
                        Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="⚠️ INGRÉDIENTS MANQUANTS"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="#E74C3C"
                                   Margin="0,0,0,10"/>

                        <ListBox x:Name="ListeIngredientsManquants"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 MaxHeight="200">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}"
                                               FontSize="14"
                                               Foreground="#E74C3C"
                                               Margin="5"/>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>

                        <Button Content="➕ Ajouter à la liste de courses"
                                Height="40"
                                Background="#E74C3C"
                                Foreground="White"
                                FontSize="14"
                                FontWeight="Bold"
                                BorderThickness="0"
                                Cursor="Hand"
                                Click="BtnAjouterListeCourses_Click"
                                Margin="0,10,0,0"/>
                    </StackPanel>
                </Border>

                <!-- Tout est disponible -->
                <Border x:Name="ToutDisponiblePanel"
                        Background="#E8F5E9"
                        CornerRadius="10"
                        Padding="20"
                        Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal"
                               HorizontalAlignment="Center">
                        <TextBlock Text="✅"
                                   FontSize="24"
                                   Margin="0,0,10,0"/>
                        <TextBlock Text="Tous les ingrédients sont disponibles !"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   Foreground="#27AE60"
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

            </StackPanel>

        </Grid>
    </ScrollViewer>

</UserControl>

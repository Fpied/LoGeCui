<Window x:Class="LoGeCui.Dialogs.AjouterIngredientRecetteDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ajouter un ingrédient" 
        Height="400" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#ECF0F1">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0"
                   Text="🥘 Ajouter un ingrédient"
                   FontSize="18"
                   FontWeight="Bold"
                   Foreground="#2C3E50"
                   Margin="0,0,0,15"/>

        <!-- Nom -->
        <StackPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Nom *"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="TxtNom"
                     Height="30"
                     FontSize="13"
                     Padding="5"/>
        </StackPanel>

        <!-- Quantité -->
        <StackPanel Grid.Row="2" Margin="0,0,0,10">
            <TextBlock Text="Quantité *"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Margin="0,0,0,5"/>
            <TextBox x:Name="TxtQuantite"
                     Height="30"
                     FontSize="13"
                     Padding="5"/>
        </StackPanel>

        <!-- Unité -->
        <StackPanel Grid.Row="3" Margin="0,0,0,10">
            <TextBlock Text="Unité *"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Margin="0,0,0,5"/>
            <ComboBox x:Name="CmbUnite"
                      Height="30"
                      FontSize="13"
                      Padding="5">
                <ComboBoxItem Content="g"/>
                <ComboBoxItem Content="kg"/>
                <ComboBoxItem Content="ml"/>
                <ComboBoxItem Content="L"/>
                <ComboBoxItem Content="pièces" IsSelected="True"/>
                <ComboBoxItem Content="cuillères à soupe"/>
                <ComboBoxItem Content="cuillères à café"/>
            </ComboBox>
        </StackPanel>

        <!-- Boutons -->
        <StackPanel Grid.Row="5"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">

            <Button Content="Annuler"
                    Width="100"
                    Height="35"
                    Margin="0,0,10,0"
                    Background="#95A5A6"
                    Foreground="White"
                    FontSize="13"
                    BorderThickness="0"
                    Click="BtnAnnuler_Click"/>

            <Button Content="Ajouter"
                    Width="100"
                    Height="35"
                    Background="#27AE60"
                    Foreground="White"
                    FontSize="13"
                    BorderThickness="0"
                    Click="BtnAjouter_Click"/>

        </StackPanel>

    </Grid>

</Window>
